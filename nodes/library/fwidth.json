{
  "id": "FWIDTH",
  "label": "FWidth",
  "category": "Math",
  "description": "Computes the sum of absolute derivatives (abs(dFdx) + abs(dFdy)). Useful for anti-aliasing or edge detection.",
  "locales": {
    "zh": {
      "FWidth": "FWidth (导数和)",
      "Input": "输入",
      "Output": "输出"
    }
  },
  "data": {
    "glsl": "void run(vec2 uv, float val, out float result) { result = fwidth(val); }\nvoid run(vec2 uv, vec2 val, out vec2 result) { result = fwidth(val); }\nvoid run(vec2 uv, vec3 val, out vec3 result) { result = fwidth(val); }\nvoid run(vec2 uv, vec4 val, out vec4 result) { result = fwidth(val); }",
    "autoType": true,
    "inputs": [
      { "id": "val", "name": "Input", "type": "float" }
    ],
    "outputs": [
      { "id": "result", "name": "Output", "type": "float" }
    ],
    "outputType": "float"
  }
}
