{
  "id": "SQRT",
  "label": "Square Root",
  "category": "Math",
  "description": "Returns the square root of A.",
  "locales": {
    "zh": {
      "Square Root": "平方根",
      "Returns the square root of A.": "返回 A 的平方根。"
    }
  },
  "data": {
    "autoType": true,
    "glsl": "void run(vec2 uv, float a, out float res) { res = sqrt(abs(a)); }\nvoid run(vec2 uv, vec2 a, out vec2 res)   { res = sqrt(abs(a)); }\nvoid run(vec2 uv, vec3 a, out vec3 res)   { res = sqrt(abs(a)); }\nvoid run(vec2 uv, vec4 a, out vec4 res)   { res = sqrt(abs(a)); }",
    "inputs": [
      { "id": "a", "name": "In", "type": "vec4" }
    ],
    "outputType": "vec4",
    "outputs": [{ "id": "res", "name": "Out", "type": "vec4" }]
  }
}