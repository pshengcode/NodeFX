{
  "id": "DEGREES",
  "label": "Degrees",
  "category": "Math",
  "description": "Converts radians to degrees.",
  "locales": {
    "zh": {
      "Degrees": "角度",
      "Converts radians to degrees.": "将弧度转换为角度。"
    }
  },
  "data": {
    "autoType": true,
    "glsl": "void run(vec2 uv, float a, out float res) { res = degrees(a); }\nvoid run(vec2 uv, vec2 a, out vec2 res) { res = degrees(a); }\nvoid run(vec2 uv, vec3 a, out vec3 res) { res = degrees(a); }\nvoid run(vec2 uv, vec4 a, out vec4 res) { res = degrees(a); }",
    "inputs": [
      { "id": "a", "name": "Rad", "type": "vec4" }
    ],
    "outputType": "vec4",
    "outputs": [{ "id": "res", "name": "Deg", "type": "vec4" }]
  }
}
