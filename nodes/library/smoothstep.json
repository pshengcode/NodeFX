{
  "id": "SMOOTHSTEP",
  "label": "Smoothstep",
  "category": "Math",
  "description": "Returns a smooth Hermite interpolation between 0 and 1.",
  "locales": {
    "zh": {
      "Smoothstep": "平滑阶梯",
      "Returns a smooth Hermite interpolation between 0 and 1.": "返回 0 和 1 之间的平滑 Hermite 插值。"
    }
  },
  "data": {
    "autoType": true,
    "glsl": "void run(vec2 uv, float edge0, float edge1, float x, out float res) { res = smoothstep(edge0, edge1, x); }\nvoid run(vec2 uv, vec2 edge0, vec2 edge1, vec2 x, out vec2 res)   { res = smoothstep(edge0, edge1, x); }\nvoid run(vec2 uv, vec3 edge0, vec3 edge1, vec3 x, out vec3 res)   { res = smoothstep(edge0, edge1, x); }\nvoid run(vec2 uv, vec4 edge0, vec4 edge1, vec4 x, out vec4 res)   { res = smoothstep(edge0, edge1, x); }",
    "inputs": [
      { "id": "edge0", "name": "Edge0", "type": "vec4", "default": [0,0,0,0] },
      { "id": "edge1", "name": "Edge1", "type": "vec4", "default": [1,1,1,1] },
      { "id": "x", "name": "In", "type": "vec4" }
    ],
    "outputType": "vec4",
    "outputs": [{ "id": "res", "name": "Out", "type": "vec4" }]
  }
}