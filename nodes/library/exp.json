{
  "id": "EXP",
  "label": "Exp",
  "category": "Math",
  "description": "Returns the natural exponentiation of the parameter (e^x).",
  "locales": {
    "zh": {
      "Exp": "指数 (Exp)",
      "Returns the natural exponentiation of the parameter (e^x).": "返回参数的自然指数 (e^x)。"
    }
  },
  "data": {
    "autoType": true,
    "glsl": "void run(vec2 uv, float a, out float res) { res = exp(a); }\nvoid run(vec2 uv, vec2 a, out vec2 res) { res = exp(a); }\nvoid run(vec2 uv, vec3 a, out vec3 res) { res = exp(a); }\nvoid run(vec2 uv, vec4 a, out vec4 res) { res = exp(a); }",
    "inputs": [
      { "id": "a", "name": "In", "type": "vec4" }
    ],
    "outputType": "vec4",
    "outputs": [{ "id": "res", "name": "Out", "type": "vec4" }]
  }
}
