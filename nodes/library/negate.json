{
  "id": "NEGATE",
  "label": "Negate",
  "category": "Math",
  "description": "Returns -A.",
  "locales": {
    "zh": {
      "Negate": "取反",
      "Returns -A.": "返回 -A。"
    }
  },
  "data": {
    "autoType": true,
    "glsl": "void run(vec2 uv, float a, out float res) { res = -a; }\nvoid run(vec2 uv, vec2 a, out vec2 res)   { res = -a; }\nvoid run(vec2 uv, vec3 a, out vec3 res)   { res = -a; }\nvoid run(vec2 uv, vec4 a, out vec4 res)   { res = -a; }",
    "inputs": [
      { "id": "a", "name": "In", "type": "vec4" }
    ],
    "outputType": "vec4",
    "outputs": [{ "id": "res", "name": "Out", "type": "vec4" }]
  }
}