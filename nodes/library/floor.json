{
  "id": "FLOOR",
  "label": "Floor",
  "category": "Math",
  "description": "Returns the largest integer less than or equal to A.",
  "locales": {
    "zh": {
      "Floor": "向下取整",
      "Returns the largest integer less than or equal to A.": "返回小于或等于 A 的最大整数。"
    }
  },
  "data": {
    "autoType": true,
    "glsl": "void run(vec2 uv, float a, out float res) { res = floor(a); }\nvoid run(vec2 uv, vec2 a, out vec2 res)   { res = floor(a); }\nvoid run(vec2 uv, vec3 a, out vec3 res)   { res = floor(a); }\nvoid run(vec2 uv, vec4 a, out vec4 res)   { res = floor(a); }",
    "inputs": [
      { "id": "a", "name": "In", "type": "vec4" }
    ],
    "outputType": "vec4",
    "outputs": [{ "id": "res", "name": "Out", "type": "vec4" }]
  }
}