{
  "id": "CLAMP",
  "label": "Clamp",
  "category": "Math",
  "description": "Constrains a value to lie between two further values.",
  "locales": {
    "zh": {
      "Clamp": "钳制",
      "Constrains a value to lie between two further values.": "将值限制在两个值之间。"
    }
  },
  "data": {
    "autoType": true,
    "glsl": "void run(vec2 uv, float a, float min, float max, out float res) { res = clamp(a, min, max); }\nvoid run(vec2 uv, vec2 a, vec2 min, vec2 max, out vec2 res)   { res = clamp(a, min, max); }\nvoid run(vec2 uv, vec3 a, vec3 min, vec3 max, out vec3 res)   { res = clamp(a, min, max); }\nvoid run(vec2 uv, vec4 a, vec4 min, vec4 max, out vec4 res)   { res = clamp(a, min, max); }",
    "inputs": [
      { "id": "a", "name": "In", "type": "vec4" },
      { "id": "min", "name": "Min", "type": "vec4", "default": [0,0,0,0] },
      { "id": "max", "name": "Max", "type": "vec4", "default": [1,1,1,1] }
    ],
    "outputType": "vec4",
    "outputs": [{ "id": "res", "name": "Out", "type": "vec4" }]
  }
}