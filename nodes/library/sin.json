{
  "id": "SIN",
  "label": "Sin",
  "category": "Math",
  "description": "Returns the sine of A.",
  "locales": {
    "zh": {
      "Sin": "正弦",
      "Returns the sine of A.": "返回 A 的正弦值。"
    }
  },
  "data": {
    "autoType": true,
    "glsl": "void run(vec2 uv, float a, out float res) { res = sin(a); }\nvoid run(vec2 uv, vec2 a, out vec2 res)   { res = sin(a); }\nvoid run(vec2 uv, vec3 a, out vec3 res)   { res = sin(a); }\nvoid run(vec2 uv, vec4 a, out vec4 res)   { res = sin(a); }",
    "inputs": [
      { "id": "a", "name": "In", "type": "vec4" }
    ],
    "outputType": "vec4",
    "outputs": [{ "id": "res", "name": "Out", "type": "vec4" }]
  }
}